# Optional credentials for publishing a dataset catalog to S3.
# Never commit real values. Boto3 also supports AWS profiles and IAM roles.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=us-east-1
MCWM_S3_BUCKET=
